# api-shared-gateway-pollate

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test api-shared-gateway-pollate` to execute the unit tests via [Jest](https://jestjs.io).
