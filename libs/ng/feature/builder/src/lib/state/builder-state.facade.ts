import { Injectable } from '@angular/core';
import { Store } from '@ngrx/store';

@Injectable({ providedIn: 'root' })
export class BuilderStateFacade {
  constructor(private store: Store) {}

  // Selectors
  // Actions
}
